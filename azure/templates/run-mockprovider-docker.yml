steps:

  - template: "azure/components/aws-assume-role.yml@common"
    parameters:
      role: "auto-ops"
      profile: "apim-dev"
      aws_account: "dev" 

  - bash: |

        export AWS_PROFILE=apim-dev
        account_id="$(aws sts get-caller-identity --query Account --output text)"
        mock_provider_repository_url=${account_id}.dkr.ecr.eu-west-2.amazonaws.com/gpconnect-infra-dev
        dockerImageName=${mock_provider_repository_url}:$(Build.BuildId)
        echo ${dockerImageName}
        make docker-deploy dockerImageName=${dockerImageName} aws_profile=apim-dev aws_account_no=${account_id}
        

    displayName: Build and Push PrismMockProvider Docker to ECR
    workingDirectory: "$(Pipeline.Workspace)/s/$(SERVICE_NAME)/PrismMockProvider"



