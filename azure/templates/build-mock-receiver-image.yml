steps:
  - bash: |
      docker build -t mock-reciever .
    displayName: Build and Push PrismMockProvider Docker to ECR
    workingDirectory: "$(Pipeline.Workspace)/s/$(SERVICE_NAME)/PrismMockProvider"
