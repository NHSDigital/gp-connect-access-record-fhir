docker_containers:
    - name: sandbox
      dockerfile: "sandbox/Dockerfile"
      path: "sandbox"
    - name: sandbox2
      dockerfile: "sandbox/Dockerfile2"
      path: "sandbox"
    - name: nginx
      dockerfile: "nginx/Dockerfile"
      path: "nginx"
